{
   "private": true,
   "workspaces": [
      "packages/*"
   ],
   "packageManager": "^npm@11.0.0",
   "scripts": {
      "dev": "turbo run dev --parallel",
      "build": "turbo run build",
      "changeset": "changeset",
      "version-packages": "changeset version",
      "release": "turbo run build --filter=@prdo-learning/docs && changeset publish"
   },
   "devDependencies": {
      "@changesets/cli": "^2.27.11",
      "turbo": "^2.3.3"
   }
}
